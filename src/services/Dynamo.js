import { AWS } from '../constants';
import 'aws-sdk/dist/aws-sdk';

export const getTagsForId = () => {
};

export const getIdsForTag = () => {
};

export const addTag = () => {
};

export const removeTag = () => {
};
